# --- Phase 0 (eval harness + naive baseline) ---
huggingface_hub>=0.25.0
transformers>=4.44.0      # tokenizer for accurate budgeting
pyyaml>=6.0

# --- Phase 1 (semantic retrieval) ---
sentence-transformers>=3.0.0   # BAAI/bge-small-en-v1.5 embeddings
numpy>=1.24.0                  # lightweight persisted vector store

# --- Phase 2 (hybrid + rerank) — uncomment when you reach it ---
# rank-bm25>=0.2.2
# chromadb>=0.5.0              # optional: swap the numpy store for Chroma at scale
